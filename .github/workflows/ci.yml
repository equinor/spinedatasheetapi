# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: ".NET CI Pipeline"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore datasheetapi/datasheetapi.csproj
    - name: Build
      run: dotnet build datasheetapi/datasheetapi.csproj --no-restore
    - name: Test
      run: dotnet test tests --no-build --verbosity normal
    # - name: "Docker Login dynatrace"
    #   run: echo ${{ parameters.dt_password }} | docker login -u ${{ parameters.dt_tenant }} ${{ parameters.dt_url }} --password-stdin
    # - name: "Docker build with OneAgent"
    #   run: docker build -f ${{ parameters.sourcesDirectory }}/${{ parameters.appName }}/Dockerfile --build-arg DYNATRACE_URL=${{ parameters.dt_url }} --build-arg DYNATRACE_TENANT=${{ parameters.dt_tenant }} --build-arg DYNATRACE_PAAS_TOKEN=${{ parameters.dt_password }} --build-arg environment=dev  ${{ parameters.sourcesDirectory }}/${{ parameters.appName }}/  -t crpia.azurecr.io/${{ parameters.appName }}:${{ parameters.buildId }}
